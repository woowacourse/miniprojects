const WOOTECOBOOKAPP = (function () {
    'use strict';

    const controller = function () {
        const init = function () {

        }

        return {
            init: init
        }
    }


    const service = function () {
        return {
        }
    }

    const init = function () {
    };

    return {
        init: init,
    };
})();

WOOTECOBOOKAPP.init();
